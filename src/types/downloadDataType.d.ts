export interface XMLdownloadDataType {
    downloadData: {
        videosUrl: string,
        format: string,
        output: string
    }
}
export interface downloadDataType {

    videosUrl: string[],
    format: string,
    output: string

}